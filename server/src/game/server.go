package game

// This is just lobby

/*
import (
    "local/hansa/database"
)

type Server struct {
    db *database.DB
}

func NewServer(db *database.DB) *Server {
    &Server{
        db: db,
    }
}

func (s *Server) CreatGame() 
*/
