package user

/*
function GetA

// Break dependency cycle on table package, this is TableServer.
type TableResolver interface {
    GetName(int) string
}

// Break dependency on league package, this is LeagueServer.
type LeagueResolver interface {
    GetName(int) string
    GetColor(int) string
    GetSign(int) string
}
*/
